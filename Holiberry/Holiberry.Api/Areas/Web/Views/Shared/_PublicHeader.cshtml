<!-- BEGIN: Header-->
    <nav class="header-navbar navbar-expand-lg navbar navbar-fixed align-items-center navbar-brand-center sticky-top" data-nav="brand-center" style="background-color: white">
        <div class="navbar-wrapper">
            <div class="navbar-container content">
                <div class="navbar-collapse d-flex justify-content-center align-items-center text-center" id="navbar-mobile">
                    <div class="mr-auto float-left bookmark-wrapper d-flex align-items-center">
                        <ul class="nav navbar-nav">
                            @if (ViewBag.isIndexPage != true)
                            {
                                <li class="nav-item">
                                    <div class="nav-item-wrapper cursor-pointer single-item">
                                        <a class="nav-link text-nowrap mr-75" style="color: #000;" href="@Url.Action("Index", "P_Home", new { area = AreasConfig.Public })">
                                            <span class="menu-icon align-middle mr-50">
                                                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                                    <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
                                                    <polyline points="9 22 9 12 15 12 15 22"></polyline>
                                                </svg>
                                            </span>
                                            <span class="menu-title align-middle d-none d-md-inline">Strona główna</span>
                                            <div class="menu-title align-middle d-block d-md-none font-small-1 ml-n50 px-75 pt-25">Strona główna</div>
                                        </a>
                                    </div>
                                </li>
                            }
                            @if (ViewBag.isIndexPage == true)
                            {
                                <li style="width:100px"></li>
                            }
                        </ul>
                    </div>
                    <div class="mr-auto navbar-header d-xl-block d-none">
                        <a href="@Url.Action("ViewLandingPage", "P_Home", new { area = AreasConfig.Public })" class="navbar-brand">
                            <img src="/images/logo.svg" style="max-height:40px;" alt="">
                        </a>
                    </div>
                    <div class="ml-auto float-right d-flex align-items-center">
                        <ul class="nav navbar-nav">
                            @if (User.Identity?.IsAuthenticated == true && User.IsInRole(UserRoles.Admin))
                            {

                                <li class="nav-item">
                                    <a href="@Url.Action("ViewAdminDashboard", "A_Dashboard", new { area = AreasConfig.Admin })" class="px-1 text-dark py-75 mr-25 text-nowrap">
                                        <i class="fa fa-user mr-50"></i><span class="menu-title">Panel</span>
                                    </a>
                                </li>
                                <li class="nav-item  d-none d-md-block">
                                    <a href="@Url.Action("Logout", "P_Account", new { area = AreasConfig.Public })" class="px-1 text-dark py-75 mr-25 text-nowrap">
                                        <i class="fa fa-sign-out mr-50"></i><span class="menu-title">Wyloguj</span>
                                    </a>
                                </li>
                            }
                            else
                            {
                                <li class="nav-item">
                                    <a href="@Url.Action("Login", "P_Account", new { area = AreasConfig.Public })" class="px-1 text-dark py-75 mr-25 text-nowrap">
                                        <i class="fa fa-sign-in mr-50"></i><span class="menu-title">Zaloguj</span>
                                    </a>
                                </li>
                                <li class="nav-item d-none d-md-block">
                                    <a href="@Url.Action("Register", "P_Account", new { area = AreasConfig.Public })" class="px-1 text-dark py-75 text-nowrap">
                                        <span class="menu-title">Zarejestruj</span>
                                    </a>
                                </li>
                            }
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    @*<nav class="navbar navbar-expand-sm sticky-top navbar-light bg-light">
        <div class="container">
            <a class="navbar-brand" href="#">Brand</a>
            <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbar1">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbar1">
                <ul class="navbar-nav">
                    <li class="nav-item active">
                        <a class="nav-link" href="#">Link</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Link</a>
                    </li>
                </ul>
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="#">Link</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>*@